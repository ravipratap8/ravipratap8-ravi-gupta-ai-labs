// Supabase server client (PLACEHOLDER) — for Route Handlers / Server Components.
// import { createServerClient } from '@supabase/ssr'
// Reads cookies for the authenticated session.

export function createClient() {
  throw new Error('Supabase server client not configured (placeholder).')
}
