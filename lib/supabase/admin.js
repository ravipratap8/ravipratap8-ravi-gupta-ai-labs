// Supabase admin client (PLACEHOLDER) — uses the service role key. Server-only.
// NEVER expose SUPABASE_SERVICE_ROLE_KEY to the browser.

export function createAdminClient() {
  throw new Error('Supabase admin client not configured (placeholder).')
}
